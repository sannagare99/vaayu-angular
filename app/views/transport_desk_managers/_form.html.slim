= simple_form_for @user, html: {'data-validate' => 'form', :class => 'modal-form', id: "form-transport_desk_managers"}, url: url, remote: true, method: action do |f|
  .row
    .col-md-4
      = f.input :f_name, label: 'First Name', required: true
    .col-md-4
      = f.input :m_name, label: 'Middle Name'
    .col-md-4
      = f.input :l_name, label: 'Last name', required: true
  .row
    .col-md-4.col-md-offset-4
      = f.input :email, label: 'Email', required: true
    .col-md-4
      = f.simple_fields_for :entity_attributes, @user.entity do |ui|
        = ui.association :employee_company, prompt: false, label: 'Company', prompt: "Choose a Company"
  = f.simple_fields_for :entity_attributes, @user.entity do |ui|
    .row
      .col-md-4.group-label.text-right
        span.field-label
          | Transport Desk Manager attributes:
      .col-md-4
        = f.input :phone, as: :tel, label: 'Phone', required: true
